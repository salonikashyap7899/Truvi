{
  "name": "truvi-mern",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm ci --prefix client && npm --prefix client run build && npm ci --prefix server && npm --prefix server run build",
    "start": "npm --prefix server run start"
  }
}
